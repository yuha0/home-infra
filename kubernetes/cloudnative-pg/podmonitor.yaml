apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: db
spec:
  namespaceSelector:
    any: true
  podMetricsEndpoints:
  - bearerTokenSecret:
      key: ""
      name: ""
    port: metrics
  selector:
    matchLabels:
      cnpg.io/podRole: instance
