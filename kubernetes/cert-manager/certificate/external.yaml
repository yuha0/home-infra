apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: external-yuha0-com
  namespace: ingress-nginx
spec:
  secretName: wildcard-external-yuha0-com
  dnsNames:
  - "*.external.yuha0.com"
  issuerRef:
    name: letsencrypt
    group: cert-manager.io
    kind: ClusterIssuer
