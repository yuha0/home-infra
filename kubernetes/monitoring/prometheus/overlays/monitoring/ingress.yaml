apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prom
spec:
  rules:
  - host: prom-monitoring.internal.yuha0.com
    http:
      paths:
      - backend:
          service:
            name: prom-monitoring
            port:
              number: 9090
        path: /
        pathType: Prefix
