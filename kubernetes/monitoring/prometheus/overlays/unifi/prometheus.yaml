apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prom
spec:
  externalLabels:
    prometheus: unifi
  replicas: 1
  resources:
    limits:
      cpu: 100m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 200Mi
  serviceMonitorNamespaceSelector:
    matchLabels:
      kubernetes.io/metadata.name: unifi
