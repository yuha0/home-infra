apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  labels:
    app: plex
    use-cloudflare-solver: "true"
  name: wildcard
  namespace: ingress-nginx
spec:
  dnsNames:
  - '*.example.com'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: wildcard
