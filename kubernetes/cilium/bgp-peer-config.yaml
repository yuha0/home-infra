apiVersion: cilium.io/v2
kind: CiliumBGPPeerConfig
metadata:
  name: default
spec:
  gracefulRestart:
    enabled: true
    restartTimeSeconds: 30
  families:
  - afi: ipv4
    safi: unicast
    advertisements:
      matchLabels:
        advertise: bgp
