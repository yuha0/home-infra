namePrefix: dashboard-postgres-
commonAnnotations:
  grafana-provisioning-directory: postgres
generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - cloudnativepg.json
  name: cloudnativepg
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
- includeSelectors: true
  pairs:
    grafana-provisioning: dashboard
