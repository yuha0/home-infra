namePrefix: dashboard-hardware-
commonLabels:
  grafana-provisioning: dashboard
commonAnnotations:
  grafana-provisioning-directory: hardware
generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: ups
  files:
  - ups.json
