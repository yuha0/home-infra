apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/instance: vector

resources:
- deployment.yaml
- ingress.yaml
- service.yaml

configMapGenerator:
- name: vector
  files:
  - vector.yaml
