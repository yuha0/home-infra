commonLabels:
  app.kubernetes.io/name: unifi
  app.kubernetes.io/component: exporter

resources:
- service.yaml
- servicemonitor.yaml
- deployment.yaml

images:
- name: unpoller
  newName: golift/unifi-poller
  newTag: "2.1.3"

configMapGenerator:
- name: config
  files:
  - up.conf
