- hosts: all
  become: yes
  # # only need this for the first run before sudo is installed
  # become_method: su
  roles:
  - common
  tags:
  - common

- hosts: all
  become: yes
  roles:
  - k0s
  tags:
  - k0s
